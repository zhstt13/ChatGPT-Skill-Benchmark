# Canonical Benchmark Data

Start at `manifest.json`. The validator deliberately reads only this directory plus the registered Candidate Skill paths, so historical templates cannot affect a score by accident.

